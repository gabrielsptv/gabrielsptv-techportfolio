// Importe o Bootstrap JavaScript
import 'bootstrap';

// Importe o Bootstrap CSS (se ainda não estiver importado via CSS)
import 'bootstrap/dist/css/bootstrap.min.css';

// Importe o jQuery (se necessário)
import $ from 'jquery';
window.jQuery = $;
window.$ = $;
